upstream django_project {
    server backend:8000;
}

server {

    listen 80;

    location / {
        include proxy_params;
        proxy_pass http://django_project;
    }

    location /static/ {
        alias /yt/static/;
    }

    location /media/ {
        alias /yt/media/;
    }
}